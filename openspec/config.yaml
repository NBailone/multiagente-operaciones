# openspec/config.yaml
# Auto-generated by sdd-init — 2026-06-18 (re-initialized)
schema: spec-driven

context: |
  Project: Multiagente — Maritime shipping operations automation suite
  Stack: Python 3.9+, CustomTkinter (desktop GUI), PyInstaller (packaging)
  Architecture: Monolithic single-file app (ui_app.py ~10K lines) + utils/ helpers
  OCR: Tesseract (pytesseract) + PaddleOCR (sidecar engines/)
  Integrations: IMAP/email automation, Excel (openpyxl/xlrd), DeepSeek AI, OpenRouter/Gemini API Vision
  Security: Encrypted JSON config (enc:: prefix), dotenv for secret key
  Style: Rioplatense Spanish UI, maritime/logistics domain
  Persistence: openspec (files only, git-tracked)

testing:
  strict_tdd: false
  detected_at: "2026-06-18"
  test_runner: null
  test_framework: null
  layers:
    unit:
      available: false
      tool: null
    integration:
      available: false
      tool: null
    e2e:
      available: false
      tool: null
  coverage:
    available: false
    command: null
  quality:
    linter:
      available: false
      command: null
    type_checker:
      available: false
      command: null
    formatter:
      available: false
      command: null

rules:
  proposal:
    - Include risk assessment for changes affecting monolithic core
    - Identify files affected (ui_app.py line ranges when applicable)
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
    - Specs in Spanish (project domain language) or English per artifact scope
  design:
    - Document monolith module boundaries even when adding to ui_app.py
    - Prefer extraction to utils/ over monolithic growth
    - Include IMAP/Excel edge cases (locked files, connection drops)
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
    - Always note ui_app.py line ranges when known
  apply:
    - Follow existing Spanish comments and code patterns
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: "pyinstaller Sistema_Automatizacion.spec"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
    - Ensure removed secrets references are cleaned from all specs
